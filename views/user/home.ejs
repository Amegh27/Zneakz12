<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ZNEAKZ</title>
  <!-- In your <head> -->
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">

  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500&family=Oswald:wght@400&display=swap"
    rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Anton&family=Bebas+Neue&display=swap" rel="stylesheet">

  <!-- Bootstrap CSS -->
  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Font Awesome (for heart icon) -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

  <style>
    /* Top Navbar */
    body {
      background-color: #f3efef;
      color: #edecec;
    }

    .navbar-top {
      background-color: #1f1f1f;
      color: #f5e6e6;
      font-size: 14px;
      padding: 8px 0;
      text-align: center;
      letter-spacing: 0.5px;
    }

    .navbar-brand {
      font-family: 'Anton', sans-serif;
      font-size: 1.9rem;
      letter-spacing: 2px;
      color: #111;
    }

    .nav-link {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 1px;
      color: #333;
      transition: color 0.2s ease-in-out;
    }

    .nav-link:hover {
      color: #e91e63;
    }



    /* Hero Text */
    .hero-text {
      font-family: 'DM Serif Display', serif;
      font-size: 2.7rem;
      font-style: italic;
      font-weight: 400;
      text-align: center;
      color: #1a1a1a;
      max-width: 700px;
      margin: 0 auto;
      line-height: 1.4;
      letter-spacing: 0.3px;
    }



    .hero-image {
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      object-fit: cover;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    /* Search Box */
    .search-box {
      max-width: 320px;
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 6px 12px;
      font-size: 14px;
    }

    .form-label {
      color: #111;
    }

    /* Product Card */
    .product-cart-wrap {
      border: 1px solid #e0e0e0;
      border-radius: 12px;
      overflow: hidden;
      transition: all 0.3s ease;
      background-color: #fff;
      position: relative;
    }

    .product-cart-wrap:hover {
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
      transform: translateY(-5px);
    }

    .new-arrivals-heading {
      font-family: 'Anton', sans-serif;
      /* Matches your brand style */
      font-size: 2rem;
      letter-spacing: 1px;
      color: #111;
      /* Deep black for contrast */
      text-transform: uppercase;
      border-bottom: 3px solid #ff3b3b;
      /* Accent underline */
      display: inline-block;
      padding-bottom: 5px;
      margin-bottom: 20px;
    }

    /* Product Image */
    .product-img img {
      width: 100%;
      height: 230px;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .product-img img:hover {
      transform: scale(1.03);
    }

    /* Product Details */
    .product-content-wrap {
      padding: 12px;
    }

    .product-content-wrap h6 {
      font-weight: 600;
      font-size: 1rem;
      color: #333;
      margin: 8px 0 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }


    .product-content-wrap h5 {
      font-weight: 700;
      color: #e60023;
      font-size: 1.1rem;
      margin-bottom: 6px;
    }

    /* Wishlist Icon */
    .wishlist-btn {
      position: absolute;
      top: 12px;
      right: 12px;
      background: #fff;
      border-radius: 50%;
      padding: 6px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      color: #e60023;
      transition: background 0.3s ease, transform 0.3s ease;
      z-index: 2;
    }


    .wishlist-btn:hover {
      background-color: #ffe6ea;
      transform: scale(1.1);
    }
  </style>
</head>

<body>

  <!-- Top Banner -->
  <div class="navbar-top">
    Summer Sale And Free Express Delivery - OFF 50%!
    <a href="#" class="text-warning text-decoration-underline">ShopNow by ZNEAKZ</a>
  </div>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="#">ZNEAKZ</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="#">MEN</a></li>
          <li class="nav-item"><a class="nav-link" href="#">WOMEN</a></li>
          <li class="nav-item"><a class="nav-link" href="#">KIDS</a></li>
        </ul>

        <form class="d-flex search-box me-3" action="/#productSection" method="GET">
          <input class="form-control me-2" type="search" name="q" placeholder="Search products..."
            value="<%= typeof query !== 'undefined' ? query : '' %>">

          <!-- Sort dropdown -->
          <select class="form-select me-2" name="sort" onchange="this.form.submit()">
            <option value="">Sort by</option>
            <option value="priceAsc" <%=sort==='priceAsc' ? 'selected' : '' %>>Price: Low to High</option>
            <option value="priceDesc" <%=sort==='priceDesc' ? 'selected' : '' %>>Price: High to Low</option>
            <option value="nameAsc" <%=sort==='nameAsc' ? 'selected' : '' %>>Name: A - Z</option>
            <option value="nameDesc" <%=sort==='nameDesc' ? 'selected' : '' %>>Name: Z - A</option>
          </select>

          <button class="btn btn-dark me-2" type="submit">
            <i class="bi bi-search"></i>
          </button>

          <% if (typeof query !=='undefined' && query.trim() !=='' ) { %>
            <a href="/" class="btn btn-outline-secondary">
              <i class="bi bi-x-lg"></i> Clear
            </a>
            <% } %>

              <!-- Preserve selectedCategory (can be an array) -->
              <% if (Array.isArray(selectedCategory)) { %>
                <% selectedCategory.forEach(cat=> { %>
                  <input type="hidden" name="category" value="<%= cat %>">
                  <% }) %>
                    <% } else if (selectedCategory) { %>
                      <input type="hidden" name="category" value="<%= selectedCategory %>">
                      <% } %>

                        <!-- Preserve priceRange -->
                        <% if (priceRange) { %>
                          <input type="hidden" name="priceRange" value="<%= priceRange %>">
                          <% } %>
        </form>
        <a href="#"><i class="bi bi-heart fs-5 me-3"></i></a>
        <a href="#"><i class="bi bi-cart fs-5 me-3"></i></a>


        <% if (typeof user !=='undefined' && user) { %>
          <div class="dropdown">
            <a class="d-flex align-items-center text-decoration-none dropdown-toggle" href="#" role="button"
              id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              <span>
                <%= user.name %>
              </span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li>
                <a class="dropdown-item" href="/profile">Profile</a>
              </li>
              <li>
                <hr class="dropdown-divider">
              </li>
              <li>
                <a class="dropdown-item text-danger" href="#" onclick="confirmLogout(event)">Logout</a>
              </li>
            </ul>
          </div>


          <% } else { %>
            <a href="/signup" class="btn btn-outline-dark me-2">Sign Up</a>
            <a href="/login" class="btn btn-outline-dark me-2">Log In</a>
            <% } %>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner" style="height: 75vh;">

      <!-- Slide 1 -->
      <div class="carousel-item active">
        <img src="/uploads/products/homepage.jpeg" class="d-block w-100 h-100" style="object-fit: contain;"
          alt="Banner 1">
      </div>

      <!-- Slide 2 -->
      <div class="carousel-item">
        <img src="/uploads/products/Homepage_8.jpeg" class="d-block w-100 h-100" style="object-fit: contain;"
          alt="Banner 2">
      </div>

    </div>

    <!-- Prev/Next Controls -->
    <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
      <span class="carousel-control-prev-icon"></span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
      <span class="carousel-control-next-icon"></span>
    </button>
  </div>

  <div>
    <img src="/uploads/products/homepage_6.jpeg" alt="Second Banner" class="img-fluid w-100 h-100"
      style="object-fit: contain;">
  </div>



  <!-- Full width container for sidebar + product -->
  <div class="container-fluid px-0">
    <div class="d-flex">

      <!-- Filter Sidebar -->
      <div class="col-md-2 bg-light p-3">

        <form method="GET" action="/#productSection">
          <h5 class="fw-bold mb-3" style="color: #111;">Filter</h5>

          <!-- Category -->
          <div class="mb-3">
            <label for="category" class="form-label">Category</label>
            <select class="form-select" name="category">
              <option value="">All</option>
              <% categories.forEach(cat=> { %>
                <option value="<%= cat.name %>" <%=selectedCategory.includes(cat.name) ? 'selected' : '' %>>
                  <%= cat.name %>
                </option>
                <% }) %>
            </select>
          </div>

          <!-- Price Range -->
          <div class="mb-3">
            <label class="form-label">Price Range</label>
            <select class="form-select" name="priceRange">
              <option value="">All</option>
              <option value="0-2000" <%=priceRange==="0-2000" ? "selected" : "" %>>Below ₹2000</option>
              <option value="0-2500" <%=priceRange==="0-2500" ? "selected" : "" %>>Below ₹2500</option>
              <option value="0-3000" <%=priceRange==="0-3000" ? "selected" : "" %>>Below ₹3000</option>
              <option value="3000-5000" <%=priceRange==="3000-5000" ? "selected" : "" %>>₹3000 – ₹5000</option>
              <option value="5000-100000" <%=priceRange==="5000-100000" ? "selected" : "" %>>Above ₹5000</option>
            </select>
          </div>

          <button type="submit" class="btn btn-dark w-100">Apply Filter</button>
        </form>
      </div>

      <!-- Product Grid -->
      <!-- Product Grid (horizontal scroll) -->
      <div id="productSection" class="flex-grow-1 overflow-auto">
        <div class="container py-4">
          <h2 class="new-arrivals-heading">New Arrivals</h2>

          <div class="d-flex flex-nowrap gap-3 overflow-x-auto pb-3">


            <% if (products && products.length> 0) { %>
              <% products.forEach(product=> { %>
                <div class="product-card" style="min-width: 280px;">
                  <div class="product-cart-wrap position-relative">
                    <div class="product-img">
                      <a href="/product-details?id=<%= product._id %>">
                        <img src="/uploads/products/<%= product.productImage[0] %>" alt="<%= product.productName %>">
                      </a>
                    </div>
                    <a href="#" class="wishlist-btn">
                      <i class="fas fa-heart"></i>
                    </a>
                    <div class="product-content-wrap text-center">
                      <h6>
                        <%= product.productName %>
                      </h6>
                      <h5>
                        <% if (product.discountPrice && product.discountPrice < product.price) { %>
                          <span class="text-muted" style="text-decoration: line-through;">₹<%= product.price %></span>
                          <span class="text-danger fw-bold">₹<%= product.discountPrice %></span>
                          <% } else { %>
                            <span class="fw-bold">₹<%= product.price %></span>
                            <% } %>
                      </h5>


                    </div>
                  </div>
                </div>
                <% }) %>
                  <% } else { %>
                    <p>No products found.</p>
                    <% } %>

          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Pagination -->
  <nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center mt-4">
      <% if (currentPage> 1) { %>
        <li class="page-item">
          <a class="page-link" href="/?page=<%= currentPage - 1 %>#productSection">Previous</a>

        </li>
        <% } else { %>
          <li class="page-item disabled">
            <span class="page-link">Previous</span>
          </li>
          <% } %>

            <% for (let i=1; i <=totalPages; i++) { %>
              <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                <a class="page-link" href="/?page=<%= i %>#productSection">
                  <%= i %>
                </a>
              </li>
              <% } %>

                <% if (currentPage < totalPages) { %>
                  <li class="page-item">
                    <a class="page-link" href="/?page=<%= currentPage + 1 %>#productSection">Next</a>
                  </li>
                  <% } else { %>
                    <li class="page-item disabled">
                      <span class="page-link">Next</span>
                    </li>
                    <% } %>
    </ul>
  </nav>
  <script>
    function confirmLogout(event) {
      event.preventDefault(); // Stop the default link behavior
      if (confirm("Are you sure you want to logout?")) {
        window.location.href = "/logout";
      }
    }
  </script>

  <%- include("../partials/user/footer") %>