<!DOCTYPE html>
<html lang="en">
<head>
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart - ZNEAKZ</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        /* Top Banner */
        .top-banner {
            background-color: #000;
            color: #fff;
            text-align: center;
            padding: 8px;
            font-size: 12px;
        }

        .top-banner a {
            color: #fff;
            text-decoration: underline;
            margin-left: 5px;
        }

        /* Header */
        header {
            background-color: #fff;
            padding: 15px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #e0e0e0;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            letter-spacing: 2px;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 30px;
        }

        nav a {
            text-decoration: none;
            color: #333;
            font-size: 14px;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .search-box {
            display: flex;
            align-items: center;
            border: 1px solid #e0e0e0;
            padding: 5px 10px;
            border-radius: 3px;
        }

        .search-box input {
            border: none;
            outline: none;
            padding: 5px;
            width: 200px;
            font-size: 13px;
        }

        .language-select {
            border: none;
            background: transparent;
            cursor: pointer;
            font-size: 13px;
        }

        .icons {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .icon-btn {
            position: relative;
            cursor: pointer;
            font-size: 18px;
        }

        .badge {
            position: absolute;
            top: -8px;
            right: -8px;
            background-color: #e74c3c;
            color: white;
            border-radius: 50%;
            width: 18px;
            height: 18px;
            font-size: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Breadcrumb */
        .breadcrumb {
            padding: 20px 50px;
            font-size: 13px;
            color: #666;
        }

        .breadcrumb a {
            color: #666;
            text-decoration: none;
        }

        /* Main Content */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px 50px;
        }

        /* Cart Table */
        .cart-table {
            background-color: #fff;
            border-radius: 5px;
            padding: 30px;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            border-bottom: 2px solid #f0f0f0;
        }

        th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
            color: #333;
        }

        td {
            padding: 25px 15px;
            border-bottom: 1px solid #f0f0f0;
        }

        .product-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .product-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 5px;
        }

        .quantity-input {
            display: flex;
            align-items: center;
            border: 1px solid #e0e0e0;
            border-radius: 3px;
            width: 80px;
        }

        .quantity-input input {
            width: 40px;
            border: none;
            text-align: center;
            padding: 8px 0;
            font-size: 14px;
        }

        .quantity-input button {
            background: transparent;
            border: none;
            padding: 8px 10px;
            cursor: pointer;
            font-size: 14px;
        }

        .quantity-input button:hover {
            background-color: #f5f5f5;
        }

        /* Bottom Section */
        .bottom-section {
            display: flex;
            justify-content: space-between;
            gap: 30px;
        }

        .left-section {
            flex: 1;
        }

        .return-btn {
            background-color: #fff;
            border: 1px solid #ddd;
            padding: 12px 30px;
            cursor: pointer;
            border-radius: 3px;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .return-btn:hover {
            background-color: #f5f5f5;
        }

        .coupon-section {
            display: flex;
            gap: 10px;
        }

        .coupon-input {
            flex: 1;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 3px;
            font-size: 14px;
        }

        .apply-btn {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 12px 30px;
            cursor: pointer;
            border-radius: 3px;
            font-size: 14px;
        }

        .apply-btn:hover {
            background-color: #c0392b;
        }

        /* Cart Total */
        .cart-total {
            background-color: #fff;
            padding: 30px;
            border-radius: 5px;
            border: 1px solid #ddd;
            min-width: 300px;
        }

        .cart-total h3 {
            margin-bottom: 20px;
            font-size: 16px;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            font-size: 14px;
            border-bottom: 1px solid #f0f0f0;
        }

        .total-row:last-of-type {
            border-bottom: 2px solid #f0f0f0;
            font-weight: 600;
            padding: 15px 0;
        }

        .checkout-btn {
            width: 100%;
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 15px;
            cursor: pointer;
            border-radius: 3px;
            font-size: 14px;
            margin-top: 20px;
        }

        .checkout-btn:hover {
            background-color: #c0392b;
        }

        .update-cart-btn {
            background-color: #fff;
            border: 1px solid #ddd;
            padding: 12px 30px;
            cursor: pointer;
            border-radius: 3px;
            font-size: 14px;
            float: right;
            margin-top: 20px;
        }

        .update-cart-btn:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <!-- Top Banner -->
    <div class="top-banner">
        Survival Sale And Free Express Delivery - OFF 50%! 
        <a href="#">ShopNow</a>
    </div>

    <!-- Header -->
    <header>
        <div class="logo">ZNEAKZ</div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="#">Contact</a></li>
                <li><a href="#">About</a></li>
            </ul>
        </nav>
        <div class="header-right">
            <div class="search-box">
                <input type="text" placeholder="What are you looking for?">
                <span>🔍</span>
            </div>
            
            <div class="icons">
                <div class="icon-btn">
                    ❤️
                </div>
                <div class="icon-btn">👤</div>
            </div>
        </div>
    </header>

 <!-- Breadcrumb -->
<div class="breadcrumb">
  <a href="/">Home</a> / <span>Cart</span>
</div>

<!-- 🛒 Cart Section -->
<div class="container">
  <div class="cart-table">
    <table>
      <thead>
        <tr>
          <th>Product</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Total</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
  <% if (cart && cart.items && cart.items.length > 0) { %>
    <% cart.items.forEach(item => { %>
      <tr class="<%= item.product.stock === 0 ? 'text-muted opacity-50' : '' %>">
        <td class="product-info">
          <img src="<%= item.product.images[0] %>" class="product-img" alt="">
          <div>
            <strong><%= item.product.name %></strong><br>
            <small><%= item.product.brand %></small>
          </div>
        </td>

        <td>₹<%= item.product.discountPrice || item.product.price %></td>

        <td>
          <% if (item.product.stock === 0) { %>
            <span class="badge bg-danger">Out of Stock</span>
          <% } else { %>
            <div class="quantity-input">
              <button class="decrease-btn" data-id="<%= item.product._id %>">−</button>
              <input type="text" value="<%= item.quantity %>" readonly>
              <button class="increase-btn" data-id="<%= item.product._id %>">+</button>
            </div>
          <% } %>
        </td>

        <td>₹<%= (item.product.discountPrice || item.product.price) * item.quantity %></td>

        <td>
          <button class="btn btn-sm btn-outline-danger remove-btn" data-id="<%= item.product._id %>">
            <i class="fa fa-trash"></i>
          </button>
        </td>
      </tr>
    <% }) %>
  <% } else { %>
    <tr>
      <td colspan="5" class="text-center">Your cart is empty</td>
    </tr>
  <% } %>
</tbody>

    </table>
  </div>
</div>

<%- include("../partials/user/footer") %>
