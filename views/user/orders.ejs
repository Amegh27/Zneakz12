<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light py-5">
<div class="container">
  <h3>Order Details - <%= order.orderID %></h3>
  <p>Status: <strong><%= order.status %></strong></p>
  <p>Order Date: <%= order.createdAt.toLocaleString() %></p>

  <h5 class="mt-4">Items:</h5>
  <table class="table table-bordered bg-white">
    <thead>
      <tr>
        <th>Product</th>
        <th>Size</th>
        <th>Quantity</th>
        <th>Price</th>
      </tr>
    </thead>
    <tbody>
      <% order.items.forEach(item => { %>
        <tr>
          <td><%= item.product.productName %></td>
          <td><%= item.size %></td>
          <td><%= item.quantity %></td>
          <td>₹<%= item.price %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <p class="mt-3"><strong>Total: ₹<%= order.totalAmount %></strong></p>

  <a href="/orders" class="btn btn-outline-dark mt-3">Back to Orders</a>
</div>
</body>
</html>
